#pragma once

/// C includes
#include <gmp.h>

/// Custom includes
#include "integer.hpp"

namespace algorithms {
	
	/// Nested sums
	
	void recursive_nested_sums(size_t a, size_t b, size_t k, integer& r);
	
}

